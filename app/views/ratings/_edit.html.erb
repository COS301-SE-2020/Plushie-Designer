<div class="edit_ratings">
    <%= simple_form_for([@toy, @rating]) do |f| %>
    <div class="row rating-form">
        <div class="col">
            <%= f.input :description, :id => "rate-desc1", :class => "rating-description", input_html: { maxlength: 250 } %>
        </div>
        <div class="col">
            <%= f.input :value, :id => "rate-val1", :class => "rating-value" %>
        </div>
        <%= f.submit :id => "rating-btn", :class => "btn submit-button" %>
    </div>
    <% end %>
</div>